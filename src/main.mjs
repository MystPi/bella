// This file will be copied to `build/dev/javascript/bella/main.mjs` after running
// `gleam build`, where it can be used as the entrypoint for tools like `deno compile`.

import { main } from './bella.mjs';

main();
